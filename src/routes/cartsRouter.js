const { Router } = require('express');
const router = Router(); 
// rutas de Carrito
router.post('/api/carrito')
router.delete('/api/carrito')
router.get('/api/carrito')
router.post('/api/carrito')
router.delete('/api/carrito')

module.exports = router;